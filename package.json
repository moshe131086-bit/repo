{
  "name": "global-price-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run build --prefix frontend && node backend/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
